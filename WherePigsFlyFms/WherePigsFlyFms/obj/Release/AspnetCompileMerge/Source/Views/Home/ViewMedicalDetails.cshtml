@model WherePigsFlyFms.ViewModels.FarmViewModel

<br />
@Html.HiddenFor(model => model.Animal.Id)
<div class="panel panel-primary">
    <div class="panel-heading"><h3 style="text-align:center;">Medical History</h3></div>
    <div class="panel-body">
        <br />
        <a href="myModal" class="btn btn-primary"  data-toggle="modal" data-target="#myModal">ADD VACCINE</a>
        <br />
        <br />
        <table class="table table-striped table-condensed table-hover table-bordered">
            <tr>
                <th>Vaccine Date</th>
                <th>Type</th>
                <th>Notes</th>
            </tr>
            @foreach (var item in Model.Vaccines)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.VaccineDate)</td>
                    <td>@Html.DisplayFor(modelItem => item.VaccineName)</td>
                    <td>@Html.DisplayFor(modelItem => item.VaccineNotes)</td>
                </tr>
            }
        </table>
    </div>
</div>


<!-- Modal -->
<div id="myModal" class="modal fade" role="dialog">
    <div class="modal-dialog modal-lg">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header bg-primary">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">REGISTER NEW MEDICAL HISTORY</h4>
            </div>
            <div class="modal-body">
                @Html.Partial("_NewMedicalHistory", Model)
            </div>
        </div>
    </div>
</div>
